# Staging

This directory contains the staged shellcode delivery logic used in the thesis.

The staging logic fetches the second-stage shellcode at runtime, which is then executed.

The staged approach was evaluated to analyze its impact on static and dynamic detection behavior.
